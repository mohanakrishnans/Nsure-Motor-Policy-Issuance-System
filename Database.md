QUOTATION MASTER:
```
CREATE TABLE EIS_MST_OTS_CONTACT_TYPE(NUM_CONTACT_ID NUMBER, VCH_CONTACT_NAME VARCHAR2(100), CHR_ACTIVE_STATUS CHAR(1),
        DTT_CREATION_DATE DATE, NUM_CREATED_BY NUMBER);
        
INSERT INTO
   EIS_MST_OTS_CONTACT_TYPE 
VALUES
   (
      1, 'INDIVIDUAL', 'Y', SYSDATE , 1 
   )
;
```
ENDORSEMENT TYPE TABLE:
```
CREATE TABLE EIS_MST_OTS_ENDORSEMENT_TYPE(NUM_ENDORSEMENT_TYPE_ID NUMBER NOT NULL PRIMARY KEY, VCH_ENDORSEMENT_TYPE_NAME VARCHAR(50));

INSERT INTO
   EIS_MST_OTS_ENDORSEMENT_TYPE 
values
   (
      1, 'jpj'
   )
;

INSERT INTO
   EIS_MST_OTS_ENDORSEMENT_TYPE 
values
   (
      2, 'non_jpj'
   )
;
```
SUB TYPE CREATION: 
```
CREATE TABLE EIS_MST_OTS_ENDO_SUBTYPE( NUM_SUBTYPE_ID NUMBER NOT nULL UNIQUE, VCH_SUBTYPE_NAME VARCHAR(100), 
NUM_ENDOr_TYPE NUMBER , FOREIGN KEY(NUM_ENDOR_TYPE) REFERENCES EIS_MST_OTS_ENDORSEMENT_TYPE(NUM_ENDORSEMENT_TYPE_ID));
```
